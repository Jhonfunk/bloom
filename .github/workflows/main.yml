name: CI

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: run
        env:
          name_of_the_one: ${{ NAME_OF_THE_ONE }}
        run: echo "$NAME_OF_THE_ONE"
